{{! Enlace para añadir al carrito }}

<h1>{{title}}</h1>

<div>
  <h2>{{product.title}}</h2>
  <p>Description: {{product.description}}</p>
  <p>Price: {{product.price}}</p>
  <p>Stock: {{product.stock}}</p>
  <p>Thumbnail: <img src="{{product.thumbnail}}" alt="Thumbnail" /></p>
  <button id="returnButton"><a href="/products/">Volver</a></button>
</div>
{{#if cartId}}
  <button id="addToCartButton">Añadir al carrito</button>
{{/if}}

<script>
  const addToCartButton = document.getElementById('addToCartButton'); if
  (addToCartButton) { addToCartButton.addEventListener('click', async () => {
  try { const response = await fetch(`/api/carts/{{cartId}}/product/{{product._id}}`,
  { method: 'POST' }); if (response.ok) { console.log('Producto añadido al
  carrito'); } else { console.error('Error al añadir el producto al carrito'); }
  } catch (error) { console.error('Error al realizar la solicitud POST', error);
  } }); }
</script>